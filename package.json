{
    "name": "nft-collection",
    "version": "1.0.0",
    "description": "Simple ERC-721 NFT collection for tests",
    "type": "commonjs",
    "devDependencies": {
        "hardhat": "^2.12.7",
        "@nomiclabs/hardhat-ethers": "^2.1.1",
        "@nomicfoundation/hardhat-chai-matchers": "^1.0.0",
        "chai": "^4.3.7"
    },
    "dependencies": {
        "ethers": "^5.7.2"
    },
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "docker:build": "docker build -t nft-contract .",
        "docker:test": "docker run --rm nft-contract"
    }
}